# QA Automation Project

## Что это?

Это **пример фреймворка для автоматизированного тестирования**, который был разработан для автоматизированного UI-тестирования на примере [demoqa.com](https://demoqa.com), также REST API того же сайта и бесплатной части REST API [reqres.in](https://reqres.in) 

Разработанная система позволяет:
- Автоматизировать рутинные проверки
- Запускать тесты в разных окружениях
- Получать детализированные отчеты
- Интегрироваться в процесс разработки

## Основные возможности:
- **UI-тестирование** всех ключевых разделов demoqa.com (Elements, Forms, Alerts, Book Store и др.)
- **API-тестирование** REST-сервисов (bookstore API, reqres.in)
- **Параллельный запуск** в Docker-контейнерах через Selenoid
- **Генерация Allure-отчетов** с графиками, шагами и скриншотами
- **CI/CD интеграция** через Jenkins с параметризованными пайплайнами

## Основные достижения:
✅ **Больше 30 UI-тестов** (при учете параметризированных тестов за один) покрывают основные пользовательские сценарии  
✅ **Больше 10 API-тестов** проверяют корректность работы REST-сервисов  
✅ **Page Object + Component паттерны** для чистого и поддерживаемого кода  
✅ **Детализированная отчетность** с шагами, фичами, эпиками, стори, скриншотами, возможностью записи видеодля анализа  

## Кейпабилити 

### Быстрые сценарии использования:
1. **Регрессионное тестирование** перед релизом — запуск всех тестов одной командой
2. **Smoke-тестирование** новой функциональности — запуск только определенной группы
3. **Локализация багов** — изолированный запуск конкретного теста
4. **Интеграция в CI/CD** — автоматический прогон при каждом коммите
5. **Демонстрация покрытия** — наглядные отчеты для стейкхолдеров

### Технические "суперсилы":
- **Адаптивность**: Запуск с разными параметрами (браузер, разрешение, таймауты)
- **Устойчивость**: Повторные попытки, явные ожидания, обработка исключений
- **Масштабируемость**: Легко добавлять новые тесты и страницы
- **Прозрачность**: Понятные логи, скриншоты на падениях, видео запись сессий
- **Переносимость**: Работает локально, в Docker и на Jenkins-агентах

## 🛠️ Технологический стек 

### ☕ **Java 17** — база
- 🛡️ Обеспечивает стабильность, является стандартом в области автоматизированного тестирование

### 🎨 **Selenide** — для UI-тестов
- ✨ Необходим для упрощения работы с требующим излишней настройки и бойлерплейта чистым `Selenium Webdriver`

### 🌐 **RestAssured** — для API-тестов
- 📡 Стандарт в тестировании REST API, позволяющий писать чистый, гибкий, хорошо читаемый код

### 🧪 **JUnit 5** — тестовый движок
- ⚙️ Обеспечивает группировку тестов по тегам, параллелизм в запуске при использовании Selenoid,
- 🔀 а также является стандартом в области тестирования

### 📊 **Allure** — отчетность
- 📈 Обеспечивает презентабельность результатов и позволяет точно выявлять, за счет скриншотов
- 🎥 и опционально видео, моменты ошибки, а также анализировать поведение приложения во время тестирования

### 🐳 **Docker Compose + Selenoid** — изоляция окружения
- 🚀 Необходимы для масштабируемости, упрощения развертывания и увелечения портативности проекта, кроме того
- 🔧 решают многие проблемы с совместимостью между разными архитектурами машин

### 🔄 **Jenkins** — CI/CD
- 🤖 Позволяет тонко настраивать пайплайн тестированияи и автоматизировать их прогон.
- 🌐 Находится в одной сети с Selenoid, что позволяет удобно совмещать их

### 📦 **Maven** — сборка и зависимости
- 🏗️ Классический стандартный сборщик
- ❓ Почему не Gradle? Потому что Maven

### 🔧 **Вспомогательные библиотеки**
- 🎭 **Lombok*** - для автогенерации бойлерплейта
- ✅ **Hamcrest** — для читаемых ассертов (`assertThat(...)`)
- 📝 **Jackson** — для работы с JSON в API-тестах
- 🎮 **WebDriverManager** — автоматическое управление драйверами
- 📸 **Allure-Selenide** — интеграция Allure с Selenide

## Запуск и развертывание

### 1. Быстрый старт

**Требования к системе:**
- Java 17+
- Maven 3.9+ 
- Git (хотя можно и скачать как zip при желании 🥶) 

**Локальный запуск:**
```bash
# Клонирование проекта
git clone https://github.com/ghimik/QA-Automation-Project.git
cd QA-Automation-Project

# Запуск всех UI-тестов
mvn clean test -Dgroup=ui -Dbase.url=https://demoqa.com

# Или только API-тестов
mvn clean test -Dgroup=api
```

**Что произойдет:**
- Скачаются все зависимости (в первый раз)
- Запустятся Chrome в headless-режиме
- Выполнятся тесты
- Сгенерируются Allure-отчеты в `allure-results`

### 2. Основные флаги JVM 

Мы используем систему properties для гибкой настройки. Вот самые полезные:

| Флаг | Default значение | Использование |
|------|----------------------|-------------|
| `-Dgroup=` | — | Группа тестов: `ui`, `api` или не указывать для всех |
| `-Dbrowser=` | `chrome` | Браузер: `chrome`, `firefox`, т.д.|
| `-DbrowserVersion=` | последняя | Версия браузера: `120.0`, `124.0` и т.д. |
| `-Dselenide.headless=` | `true` | Запуск без GUI: `true`/`false` |
| `-Dselenide.browserSize=` | `1920x1080` | Разрешение окна |
| `-Dselenide.timeout=` | `10000` | Таймаут ожидания элементов (мс) |
| `-Dselenide.pageLoadTimeout=` | `20000` | Таймаут загрузки страницы (мс) |
| `-Dselenide.remote=` | — | URL Selenoid (например, `http://localhost:4444/wd/hub`) |


### 3. Развертка через Docker Compose


```bash
# Клонирование проекта
git clone https://github.com/ghimik/QA-Automation-Project.git
cd QA-Automation-Project

# Разверка инфраструктуры (может занять время)
docker-compose up --build

# Опционально: проверка работоспособности сервисов
docker-compose ps

# Должен быть примерно такой вывод
# selenoid      running on port 4444
# selenoid-ui   running on port 8080 
# jenkins       running on port 8081
```

**Контейнеры:**
- **Selenoid** (4444) — контейнер с браузерами для тестов
- **Selenoid UI** (8080) — веб-интерфейс для мониторинга сессий
- **Jenkins** (8081) — CI/CD сервер с предустановленными плагинами

**Доступ:**
- Selenoid UI: http://localhost:8080
- Jenkins: http://localhost:8081
  - Логин: `admin`
  - Пароль: смотрим в логах Jenkins при первом запуске:
    ```bash
    docker-compose logs jenkins | grep "InitialAdminPassword"
    ```

### 4. Настройка Jenkins

**Необходимые плагины:**
- **Blue Ocean** — современный интерфейс пайплайнов
- **Docker Pipeline** — работа с Docker из Jenkinsfile
- **Allure Jenkins Plugin** — интеграция Allure-отчетов
- **Pipeline Utility Steps** — утилиты для работы с файлами

**Настройка Credentials для reqres.in API key:**
1. В Jenkins: **Manage Jenkins** → **Credentials**
2. **System** → **Global credentials** → **Add Credentials**
3. Заполняем:
   - **Kind**: Secret text
   - **Secret**: `your-reqresin-api-key-here`
   - **ID**: `reqresin-api-key` (ВАЖНО использовать именно ЭТОТ ID!)
4. **Create**

**Проверка, что всё работает:**
1. Откройте Jenkins (http://localhost:8081)
2. Создайте **New Item** → **Pipeline**
3. В **Pipeline** выберите **Pipeline script from SCM**
4. Укажите URL вашего репозитория (или вставьте скрипт как текст)
5. Нажмите **Build Now**

### 5. Запуск тестов через Docker Compose (альтернативный способ) без Jenkins

```bash
# Поднимаем selenoid
docker-compose up -d selenoid selenoid-ui

# Запускаем тесты, указывая remote, например
mvn clean test \
  -Dselenide.remote=http://localhost:4444/wd/hub \
  -Dselenide.browser=chrome \
  -Dselenide.browserVersion=120.0 \
  -Dgroup=ui

```


